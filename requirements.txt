# Intelligent Candidate Ranker — requirements (Python 3.9+)

# Core ranking with the default TF-IDF backend uses the Python standard library
# only — `python src/rank.py --backend tfidf` needs nothing below.

# Semantic embeddings backend (precompute.py + rank.py --backend stmodel):
sentence-transformers>=2.2.0
numpy>=1.24.0

# Sandbox / demo app (app.py):
streamlit>=1.28.0
pandas>=2.0.0
